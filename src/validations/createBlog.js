import * as yup from 'yup';

export const CreateBlogSchema = yup.object({
  // title: yup.string().required(),
  // content: yup.string().required(),
  // visibility: yup.boolean().required(),
  // titleImage: yup.string().required(),
});
